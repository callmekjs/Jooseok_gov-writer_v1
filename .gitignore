.env
.env.local
!.env.example
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
node_modules/
static/
dist/
*.tsbuildinfo
.superpowers/

# 시험용으로 내려받은 공공데이터 원본 (참고용, 앱 의존성 아님)
# 출처가 있는 남의 데이터를 공개 저장소에 재배포하지 않는다.
# 요약본은 docs/samples/gunwi-event-schedule-sample.md 에 출처와 함께 커밋한다.
docs/*.csv
docs/*.xlsx

# Task 13 프롬프트 측정 표본 (수십 건씩 쌓인다). 숫자는 보고서에 남기고
# 표본 자체는 커밋하지 않는다 — scripts/measure_defects.py 가 여기에 쓴다.
docs/measure/
